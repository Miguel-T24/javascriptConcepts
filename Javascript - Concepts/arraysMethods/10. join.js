// 10. join(): une todos los elementos de un array en una cadena, Podemos pasarle como parametro el caracter de separacion que debe agregar entre elemtnos

const array = ['g','d' ,'c' ,'o' , 'd', 'e', 'v'];

console.log(array.join(""));
console.log(array.join("-"));