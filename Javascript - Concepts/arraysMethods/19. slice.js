// slice(): devulve una copia de una parte dle array dentro de un nuevo array empezando por inicio hasta fin (fin no incluido). El array original no se modificara.

const array = ['a','b','c','d','e','f'];

const resultArray = array.slice(2,4);

console.log(resultArray); //['c','d']