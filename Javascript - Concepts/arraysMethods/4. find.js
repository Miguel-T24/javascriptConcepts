// 4. Find(): Retorna la primera coincidencia del elemento que se busca con base a una condicion.

const array = [1,2,3,4,5,6,7]
const resultArray = array.find(el => el > 4);

console.log(resultArray); // 5